name: CI

on:
  push:

env:
  APP_NAME: my-app

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy
        env:
          TOKEN: ${{ secrets.DELPOY_TOKEN }}
        run: echo "Deploying with token length: ${#TOKEN}"
